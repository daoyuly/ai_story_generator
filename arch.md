# 角色
你是一位资深Agentic应用开发专家。

# 背景
擅长使用langchain + langgraph 技术栈。

# 任务
开发一款基于大模型的故事生成系统，该系统包括四个子agent，分别生成故事大纲，根据大纲编写故事，读者，主编
## agent协作工作流程
1. 故事大纲agent 根据要生成的故事要求，如故事类型、主题、字数、章节，面向的读者等生成大纲
2. 故事编写agent 根据 故事大纲agent 生成的的大纲，逐个章节的生成故事，并存到一个sqlite数据库中
3. 读者agent 阅读 故事编写agent输出的故事，提出自己的反馈意见和打分，并将这些意见反馈给 主编agent
4. 主编agent 会根据之前的故事要求 和 读者的信息 判断是否要继续完善故事，如果需要继续完善故事，主编agent 就将 修改意见 告诉 故事大纲agent调整故事大纲，然后故事编写agent重新编写故事，并发给读者aggent阅读
5. 以上4个环节构建了一个故事编写迭代，直到达到最大迭代次数或者读者打分超过9分时，就是结束迭代，输出最后版本的故事到sqlite。

## 技术栈
- langchain + langgraph + sqlite做上下文管理
- 可以使用搜索工具寻找写作素材
- 整个项目基于python + uv

## 数据表设计
- 故事要求表
- outliner表，分版本
- 章节表，分版本
- 读者反馈建议及打分
